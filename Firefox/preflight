#!/usr/bin/env bash
# Automatically generated preflight script to remove 
# the application prior to installation of with this
# package.
if [ -e "$3/Applications/Firefox.app" ] ; then
    rm -Rf "$3/Applications/Firefox.app"
    exit ${?}
fi
exit 0

